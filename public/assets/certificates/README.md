# Certificate assets

Put certificate images (`.png`, `.jpg`, or `.webp`) or PDFs here. Use lowercase kebab-case names, for example `ai-for-everyone.pdf`, then set the matching `/assets/certificates/...` path in `content/content.ts`.

# VERA project assets

Put VERA screenshots here as `.png`, `.jpg`, or `.webp` files, and put the demo video here as `.mp4` or `.webm`. Use lowercase kebab-case names, for example `voice-session.png` or `voice-demo.mp4`, and reference them in `content/content.ts` as `/assets/projects/vera/<filename>`.

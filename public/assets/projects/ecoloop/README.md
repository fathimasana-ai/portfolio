# EcoLoop project assets

Put EcoLoop screenshots here as `.png`, `.jpg`, or `.webp` files. Keep the names descriptive and lowercase, for example `dashboard.png`, then update the paths in `content/content.ts`.

# Proof assets

Put award, academic and project evidence here as PDFs or images. Use lowercase kebab-case names and add the matching `/assets/proofs/...` path to the relevant `href` in `content/content.ts`.
